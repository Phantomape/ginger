# exp-20260708-030: chop forward observer wiring

- Status: `accepted_measurement_repair`
- Decision: `accepted_measurement_repair_chop_forward_observer_wiring`
- Observer smoke: `ok`, label asof `risk_on_trend`, chop days in 45d `0`
- Ledger rows: `0` (idempotent rerun delta `0`)
- Tests passed: `True`
- Strategy behavior changed: `false`

## Boundary

Do not reserve further IDs for daily chop-row materialization; the run.py step now owns it. Reopen the chop bundles only when the ledger holds >= 15 closed chop rows for a bundle (mean reversion) or >= 15 closed spreads (pairs).

## Reproduction

```powershell
.\.venv\Scripts\python.exe -B quant\experiments\exp_20260708_030_chop_forward_observer_wiring.py
```
