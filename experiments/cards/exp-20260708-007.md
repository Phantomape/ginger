# exp-20260708-007: estimate revision atomic-write EINVAL repair

- Status: `accepted_measurement_repair`
- Decision: `accepted_measurement_repair_estimate_revision_atomic_write_einval_repair`
- Ledger jsonl before/after: `True -> True`
- Ledger rows after: `1245`
- Signal match records before/after: `0 -> 65`
- Matched candidate rows after: `6`
- Outcome H3 closed rows: `0`
- Strategy behavior changed: `false`
- Accepted alpha: `false`

## Root cause

Plain truncating `open('w')` on the final jsonl fails with OSError
Errno 22 (ERROR_USER_MAPPED_FILE) on Windows while another process
holds the file memory-mapped. Writers now use the shared atomic
temp+`os.replace` path (`data_paths.atomic_write_text`).

## Boundary

Do not open another 2026-07-07 ledger regeneration or writer refactor on this surface; the atomic writer is now shared. Any recurrence of a missing daily jsonl needs the external deleter/mapper identified (capture handle owners at failure time), not another rewrite.

## Next Evidence

Closed H3/H5/H10 cash/SPY/QQQ replacement-value rows for the 2026-07-07 matched cohort, materially more non-flat matched rows, or identification of the external process that mapped/deleted the artifact.

## Reproduction

```powershell
.\.venv\Scripts\python.exe -B quant\experiments\exp_20260708_007_estimate_revision_atomic_write_einval_repair.py
.\.venv\Scripts\python.exe -m pytest quant\test_estimate_revision_ledger.py -q
```
