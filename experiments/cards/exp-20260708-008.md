# exp-20260708-008: yfinance rate-limit retry wrapper

- Status: `accepted_measurement_repair`
- Decision: `accepted_measurement_repair_yfinance_rate_limit_retry`
- Daily call sites converted: `5`
- Direct yf.download left in daily modules: `0`
- Simulated rate-limited fetch: raw=`lost`, wrapper=`recovered in 3 attempts`
- Unit tests passed: `True`
- Strategy behavior changed: `false`

## Root cause

yf.download swallows YFRateLimitError per ticker (logs `ERROR ['SPY']`,
returns an empty slice), so regime/OHLCV/trend/crypto fetches silently
degraded to 'no data' whenever Yahoo throttled. The shared wrapper
detects `yfinance.shared._ERRORS` signatures and retries with
exponential backoff under a 600s process-wide sleep budget
(`GINGER_YF_RATE_LIMIT_SLEEP_BUDGET_S`).

## Boundary

Do not re-tune retry counts/delays or re-wrap the same call sites without new evidence of loss; remaining yf.Ticker fundamental paths (earnings/info) keep their negative-cache behavior and only need call_with_rate_limit_retry if rate limits are actually observed there.

## Reproduction

```powershell
.\.venv\Scripts\python.exe -B quant\experiments\exp_20260708_008_yfinance_rate_limit_retry.py
.\.venv\Scripts\python.exe -m pytest quant\test_yf_rate_limit_retry.py -q
```
