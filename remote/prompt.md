Read AGENTS.md and the required repository status documents first.

Then continue the strategy-engineering workflow from the current accepted state.
Choose one high-value, testable alpha hypothesis unless a measurement defect is
blocking it. Follow the repository gates, keep the causal variable count to one,
run the required validation, and record the result so the next agent can replay
the work.
