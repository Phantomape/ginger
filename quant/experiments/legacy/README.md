# Legacy Experiment Scripts

This directory keeps early one-off experiment and audit runners that used to
live directly under `quant/`.

New experiment runners should live in `quant/experiments/` and write logs,
tickets, and artifacts under the top-level `experiments/` tree. Files here are
kept for reproducibility of historical experiment records, not as templates for
new work.
