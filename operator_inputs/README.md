# Operator Inputs

These are the small files edited by hand before or after a daily run.

- `open_positions.json`: current holdings, account value, cash, entry dates,
  stops, and signal target prices.

Generated pipeline outputs, caches, snapshots, and backtest results stay under
`data/`.
